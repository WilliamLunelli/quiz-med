# Neutraliza um "os=linux" definido no ~/.npmrc global desta máquina.
# Esse global fazia o npm instalar os binários nativos do Rollup para Linux,
# e o build do Vite quebrava no Windows com MODULE_NOT_FOUND (rollup/native.js).
# Vazio = usa a plataforma atual (comportamento padrão, portável).
os=
